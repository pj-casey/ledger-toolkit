Use plan mode to analyze the current codebase and create a detailed implementation plan.

1. List every component, utility module, and config that should be extracted
2. Define clear file ownership boundaries (which teammate owns which files)
3. Identify dependencies between tasks (what must finish before what)
4. Estimate complexity per task (small / medium / large)

Do NOT implement anything. Output the plan so I can review it before launching an agent team.
