Create an agent team based on the plan we just discussed. Rules:

- Max 3 teammates, each with distinct file ownership
- Include all relevant context and design decisions in each teammate's spawn prompt
- Set task dependencies so blocked work waits automatically
- The lead coordinates and synthesizes — do NOT implement code directly
- Run the architecture/scaffold teammate first, then parallelize the rest
